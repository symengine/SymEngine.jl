language: julia
sudo: false
julia:
    - release
    - nightly
notifications:
    email: false
addons:
  apt:
    packages:
    - libgmp-dev
script:
    - julia -e 'Pkg.clone(pwd())'
    - julia -e 'Pkg.build("SymEngine")'
    - julia -e 'Pkg.test("SymEngine")'
